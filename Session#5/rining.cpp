#include <iostream>
// #include <algorithm>
using namespace std;

int main()
{
    // long long abc[3];

    // cin >> abc[0] >> abc[1] >> abc[2];

    // long long min = abc[0];
    // int indexOfMin = 0;
    // for (int i = 0; i <= 3 - 1; i++)
    // {
    //     if (abc[i] < min)
    //     {
    //         min = abc[i];
    //         indexOfMin = i;
    //     }
    // }
    // long long min1 = min;

    // abc[indexOfMin] = 1000000000;
    // long long min2 = abc[0];
    // for (int i = 0; i < 3; i++)
    // {
    //     if (abc[i] < min2)
    //     {
    //         min2 = abc[i];
    //     }
    // }

    // long long a, b, c;
    // cin >> a >> b >> c;

    // long long myMax = max({a, b, c});

    // cout << (a + b + c) - myMax;
    // cout << min1 + min2;
}
